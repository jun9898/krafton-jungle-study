# 방향 그래프를 인접 행렬로 표현
graph = [
    [0, 1, 1, 0, 0],  # 정점 1에서 출발하여 정점 2, 3으로 간선이 존재
    [0, 0, 1, 1, 0],  # 정점 2에서 출발하여 정점 3, 4로 간선이 존재
    [0, 0, 0, 1, 0],  # 정점 3에서 출발하여 정점 4로 간선이 존재
    [1, 0, 0, 0, 1],  # 정점 4에서 출발하여 정점 1, 5로 간선이 존재
    [0, 1, 0, 0, 0]   # 정점 5에서 출발하여 정점 2로 간선이 존재
]

# 인접 행렬 출력
for row in graph:
    print(row)

graph = [
    [0, 2, 3, 0, 0],  # 정점 1에서 출발하여 정점 2에 가중치 2, 정점 3에 가중치 3
    [0, 0, 1, 4, 0],  # 정점 2에서 출발하여 정점 3에 가중치 1, 정점 4에 가중치 4
    [0, 0, 0, 1, 0],  # 정점 3에서 출발하여 정점 4에 가중치 1
    [5, 0, 0, 0, 2],  # 정점 4에서 출발하여 정점 1에 가중치 5, 정점 5에 가중치 2
    [0, 3, 0, 0, 0]   # 정점 5에서 출발하여 정점 2에 가중치 3
]

# 인접 행렬 출력
for row in graph:
    print(row)


# 방향 그래프를 인접 리스트로 표현
graph = {
    1: [2, 3],  # 정점 1에서 출발하여 정점 2, 3으로 간선이 존재
    2: [3, 4],  # 정점 2에서 출발하여 정점 3, 4로 간선이 존재
    3: [4],     # 정점 3에서 출발하여 정점 4로 간선이 존재
    4: [1, 5],  # 정점 4에서 출발하여 정점 1, 5로 간선이 존재
    5: [2]      # 정점 5에서 출발하여 정점 2로 간선이 존재
}

# 인접 리스트 출력
for vertex, edges in graph.items():
    print(f"{vertex} -> {edges}")

    
# 가중 그래프를 인접 리스트로 표현
graph = {
    1: [(2, 2), (3, 3)],  # 정점 1에서 출발하여 정점 2로 가중치 2, 정점 3으로 가중치 3
    2: [(3, 1), (4, 4)],  # 정점 2에서 출발하여 정점 3으로 가중치 1, 정점 4로 가중치 4
    3: [(4, 1)],          # 정점 3에서 출발하여 정점 4로 가중치 1
    4: [(1, 5), (5, 2)],  # 정점 4에서 출발하여 정점 1로 가중치 5, 정점 5로 가중치 2
    5: [(2, 3)]           # 정점 5에서 출발하여 정점 2로 가중치 3
}

# 인접 리스트 출력
for vertex, edges in graph.items():
    print(f"{vertex} -> {edges}")